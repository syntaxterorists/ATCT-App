{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },

    // This is the default connection string for the local SQL Server database.

    "AllowedHosts": "*",
    "ConnectionStrings": {
        "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=ATCT_Backend;Trusted_Connection=True;"
    },

    // This is used to configure the JWT authentication for the application, login and register.
    "Jwt": {
        "Key": "W!'$GVcysdak7E3HW!'$GVcysdak7E3H",
        "Issuer": "ATCT2025",
        "Audience": "ATCT2025Users"
    }

}
