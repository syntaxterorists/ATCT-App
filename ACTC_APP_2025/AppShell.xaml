<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ATCT2025App.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ATCT2025App.Views"
    Shell.FlyoutBehavior="Disabled">

    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:HomePage}"
        Route="HomePage" />
<!--
    <ShellContent
        Title="Program"
        ContentTemplate="{DataTemplate local:ProgramPage}"
        Route="ProgramPage" />

    <ShellContent
        Title="Speakers"
        ContentTemplate="{DataTemplate local:SpeakersPage}"
        Route="SpeakersPage" />

    <ShellContent
        Title="Locations"
        ContentTemplate="{DataTemplate local:LocationsPage}"
        Route="LocationsPage" />

    <ShellContent
        Title="About"
        ContentTemplate="{DataTemplate local:AboutPage}"
        Route="AboutPage" />

    <ShellContent
        Title="Profile"
        ContentTemplate="{DataTemplate local:ProfilePage}"
        Route="ProfilePage" />
-->
    <!-- SplashPage is not included in the tab navigation, only accessed via direct routing -->

</Shell>